name       : solt
version    : 0.1.2
release    : 3
source     :
    - git|https://github.com/HarveyDevel/solt.git : v0.1.2
license    : MIT
component  : programming.tools
summary    : simple helper bash script for packager
description: |
    simple helper bash script for packager
rundeps    :
    - git
    - powerline-fonts
    - silver-searcher
setup      : |
    %meson_configure
build      : |
    %ninja_build
install    : |
    %ninja_install
